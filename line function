#include <iostream>
#include <string>
using namespace std;

void drawLine(int length) {

    for (int i = 0; i < length; i++) {
        cout << "-";
    }
}

int main() {
    int length = 5;
    drawLine(length);
    return 0;
}
